#version 430 core

// Compute shader - one thread per cube
layout(local_size_x = 4, local_size_y = 4, local_size_z = 4) in;

// Edge array: stores vertex index for each potential edge
layout(std430, binding = 0) buffer EdgeVertexBuffer {
    int edgeVertices[];  // Size: (width+1)*(height+1)*(depth+1)*3
};

layout(std430, binding = 1) buffer VertexBuffer {
    vec3 vertices[];
};

uniform int gridWidth, gridHeight, gridDepth;

void main() {
    
}